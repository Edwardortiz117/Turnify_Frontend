# Backend local
# VITE_API_BASE_URL=http://localhost:3000

# Backend desplegado (recomendado en dev: proxy Vite, sin CORS)
VITE_API_BASE_URL=
VITE_PROXY_TARGET=https://turnify-backend.ingsoftwarefesc.com

# Llamada directa al API (solo si el servidor permite tu Origin en CORS)
# VITE_API_BASE_URL=https://turnify-backend.ingsoftwarefesc.com

# Docker (build): vacío = same-origin + proxy Nginx
# VITE_API_BASE_URL=
